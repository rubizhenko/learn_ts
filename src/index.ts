// Boolean type
let isDone: boolean = false;

// Number type
let decimal: number = 12;

// String type
let str: string = "Hello";

let res: string = `${str} world`;

// Arrays
const variant1: number[] = [12, 23, 69];
const variant2: Array<number> = [12, 23, 69];

// Tuple
let coord: [number, number];

coord = [32, 12];
// Enum

enum Color {
  Red = "#ff0000",
  Green = "#00ff00",
  Blue = "#0000ff"
}

let c: Color = Color.Red;

const testObj: Object = {};
